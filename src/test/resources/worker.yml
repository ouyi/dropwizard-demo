
messageQueue:
  host: localhost
  port: 5672
  exchangeName: filename_exchange
  exchangeType: direct
  queueName: filename_queue
  routingKey: filename_key
  autoAck: false

transformerEndpoint: http://localhost:8080/transform